const initialState = {
};

export function timePeriod (state = initialState, action) {
  switch (action.type) {
    default: return state; // Do nothing, we're not interested in this type of action
  }
}
